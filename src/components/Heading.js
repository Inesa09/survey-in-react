import React from 'react';
import './Heading.css';

const Heading = ({heading}) => {
    return (
        <div style={{position: 'reletive',}}> 
            {heading}
        </div>
    )
}

export default Heading;